[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
[project]
name = "aquamarine-dredge"
version = "122.0.0"
description = "100 Formulas in 8KB - Pure Math Universe - Zero Dep"
readme = "README.md"
dependencies = []
[project.scripts]
aquamarine-dredge = "dredge.cli:main"
