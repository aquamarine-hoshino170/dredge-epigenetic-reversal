[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "aquamarine-dredge"
version = "120.0.0"
description = "Pure Python Zero-Dependency Autonomous Biophysics & Mathematical Core - Epigenetic Entropy Reversal"
readme = "README.md"
authors = [{ name = "Aquamarine Hoshino" }]
dependencies = []

[project.scripts]
aquamarine-dredge = "dredge.cli:main"
